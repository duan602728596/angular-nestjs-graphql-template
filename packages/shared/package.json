{
  "name": "@angular-nestjs-graphql-template/shared",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "build:watch": "rimraf ./dist && tsc -w"
  },
  "exports": {
    "./enum": {
      "types": "./dist/enum/index.d.ts",
      "default": "./dist/enum/index.js"
    }
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^7.0.2"
  }
}
