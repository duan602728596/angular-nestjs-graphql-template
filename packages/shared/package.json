{
  "name": "@angular-nestjs-graphql-template/shared",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "build:watch": "rimraf ./dist && tsc -w"
  },
  "exports": {
    "./*": {
      "types": "./dist/*/*.d.ts",
      "default": "./dist/*"
    }
  },
  "devDependencies": {
    "rimraf": "^6.1.3",
    "typescript": "^6.0.3"
  }
}
